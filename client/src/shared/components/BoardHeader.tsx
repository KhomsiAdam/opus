const BoardHeader = ({ title }: any) => (
  <div className="mt-[6px] flex justify-between">
    <div className="text-[24px] font-[CircularStdMedium]">{title}</div>
  </div>
);

export default BoardHeader;
